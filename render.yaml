services:
  - type: web
    name: leeway-api
    runtime: docker
    rootDir: backend
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: ORS_API_KEY
        sync: false
      - key: OCM_API_KEY
        sync: false
